// the models
const User = require('./users');

// define the relation between the models

// User.hasMany(House, {
//     foreignKey: 'userId',
//     as: 'houses'
// });

// House.belongsTo(User, {
//     foreignKey: 'userId',
//     as: 'user'
// });